# opencode instructions for this repo

You are helping me push my codes codes while I study Jquery. This repo tracks my learning journey.You will restrucure all the code according to required new files/folders if needed so each code is in its proper place and update the docs accordingly. Perform multiple commits if needed and push the code.

## Workflow (do this every time)

1. **Read all source files** in `src/` recursively. Understand what concepts they demonstrate.
2. **Read the README** and ensure the "Topics Covered" section matches the actual files. Add/remove entries as files change.
3. **Read LICENSE, CONTRIBUTING.md, CODE_OF_CONDUCT.md, SECURITY.md** - if files are missing, recreate them.
5. **Commit** with `GIT_AUTHOR_NAME="Arun Neupane"`, `GIT_COMMITTER_NAME="Arun Neupane"`, email `arunneupane0000@gmail.com`.
6. **Push** to `origin main`.

## Commit conventions

Write meaningful, conventional commit messages:
- `feat:` for new concepts/examples
- `docs:` for README or doc changes
- `chore:` for config/setup changes
- `fix:` for TypeScript error fixes

## File structure

```

```

Each file should demonstrate one concept. Keep the chai/tea theme consistent. Files should be self-contained (no cross-imports) unless demonstrating module patterns.

## Text style

- Do NOT use em dashes (—) or emojis in text responses or texts inside code or md files. Use hyphens (-) or colons (:) instead. but md syntax (not the text that is rendered but the md syntax which is not rendered) is okay to have dashes since it is not rendered. 


## Author info

- Name: Arun Neupane
- Email: arunneupane0000@gmail.com
- Portfolio: https://arunneupane.netlify.app
- LinkedIn: https://linkedin.com/in/arundada9000
- YouTube: https://youtube.com/@arundada9000
